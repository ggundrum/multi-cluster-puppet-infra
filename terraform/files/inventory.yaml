---
groups:
  - name: puppet-enterprise-nodes
    config:
      transport: ssh
      ssh:
        host-key-check: false
        user: ubuntu
        run-as: root
        private-key: /home/ubuntu/.ssh/dev-key
    targets:
      - pe-node.gg.ggundrum-aws.tsedemos.com